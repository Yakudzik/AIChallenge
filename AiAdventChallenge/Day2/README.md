🤖Telegram AI Бот с JSON-ответами (DeepSeek - OpenAI)

Telegram-бот с AI, который:
- работает в личных чатах и группах
- отвечает на сообщения в формте json
- имеет команду `/ping` для проверки работоспособности
- использует AI (DeepSeek / OpenAI API)

Проект рассчитан на **быстрый локальный запуск** и **безопасное хранение токенов**.

---
## ✨ Возможности
- 💬 Ответы AI в Telegram
- 🔄 Структурированные JSON-ответы
- 🏓 Команда `/ping` — проверка, что бот жив
- 👥 Работа в группах (по `@упоминанию`)
- 🔐 Токены **не хранятся в коде**
- 🚀 Быстрый запуск через `start.bat`

---
## 📊 Формат ответов
Бот отвечает в стандартизированном JSON-формате:
```json
{
  "id": "96f10721-a710-4307-8553-7c2c7089fa46",
  "time": "12:16:13 - 14.01.2026",
  "model": "deepseek-chat",
  "language": "ru",
  "processing_time_ms": 2428,
  "status": "success",
  "answer": "Привет!"
}

```
## 📁 Структура проекта
.
├── botWithJsonAnswer.py    # Основной скрипт бота
├── start.bat               # Скрипт запуска для Windows
├── requirements.txt        # Зависимости Python
├── tokens.py               # Файл с токенами (не в репозитории)
└── README.md               # Этот файл

---

## 🔐 Хранение токенов (ВАЖНО)

Токены **не хранятся в коде

Для работы нужен файл **`tokens.txt`**, который вы создаёте самостоятельно.

### Формат `tokens.txt`
```txt
TELEGRAM_BOT_TOKEN=123456:ABCDEF...
DEEPSEEK_API_KEY=sk-xxxxxxxx
файл должен лежать рядом с bot.py
```

## 🚀 Быстрый запуск (Windows)

### Вариант 1 — самый простой (через `start.bat`)

1. Установите **Python 3.12+**
2. Склонируйте репозиторий
3. Создайте `tokens.txt` (по примеру `tokens.example.txt`)
4. Дважды кликните **`start.bat`**
5. Готово ✅

`start.bat`:
- создаёт виртуальное окружение
- устанавливает зависимости
- запускает бота

---
### Вариант 2 — вручную (через консоль)

`pip install -r requirements.txt python bot.py`

---
## 🧪 Проверка работы бота
username бота - @WhiteRabbit121_bot

### В личном чате
Напишите боту:
`/start`
